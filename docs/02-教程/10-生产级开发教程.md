# 生产级开发教程

## 一、项目概述

本项目讲解如何将 AI Agent 从原型开发转变为生产级应用，涵盖工程化、可靠性、性能优化等关键主题。

**项目位置**：`projects/10-production-grade/`

**学习目标**：
- 掌握标准化的项目结构
- 学习配置管理和环境隔离
- 理解日志、监控和追踪
- 掌握错误处理和降级策略
- 学习测试和质量保证
- 理解性能优化和成本控制

---

## 二、项目结构

### 2.1 标准项目模板

```
ai-agent-project/
├── src/
│   ├── __init__.py
│   ├── agents/              # Agent 实现
│   │   ├── __init__.py
│   │   ├── base.py         # Agent 基类
│   │   ├── research_agent.py
│   │   └── writer_agent.py
│   ├── tools/              # 工具定义
│   │   ├── __init__.py
│   │   ├── search.py
│   │   └── database.py
│   ├── memory/             # 记忆系统
│   │   ├── __init__.py
│   │   ├── short_term.py
│   │   └── long_term.py
│   ├── utils/              # 工具函数
│   │   ├── __init__.py
│   │   ├── logger.py
│   │   ├── config.py
│   │   └── retry.py
│   ├── api/                # API 接口
│   │   ├── __init__.py
│   │   ├── routes.py
│   │   └── schemas.py
│   └── main.py             # 入口文件
├── tests/
│   ├── __init__.py
│   ├── unit/               # 单元测试
│   ├── integration/        # 集成测试
│   └── conftest.py         # pytest 配置
├── config/
│   ├── development.yaml
│   ├── staging.yaml
│   └── production.yaml
├── scripts/                # 脚本工具
│   ├── deploy.sh
│   └── migrate.py
├── docs/                   # 文档
│   ├── api.md
│   └── architecture.md
├── .env.example
├── .gitignore
├── requirements.txt
├── requirements-dev.txt
├── Dockerfile
├── docker-compose.yml
├── pyproject.toml
└── README.md
```

### 2.2 基础 Agent 类

```python
# src/agents/base.py
from abc import ABC, abstractmethod
from typing import Dict, Any, Optional
from datetime import datetime
import logging

class BaseAgent(ABC):
    """Agent 基类"""
    
    def __init__(
        self,
        agent_id: str,
        config: Dict[str, Any],
        logger: Optional[logging.Logger] = None
    ):
        self.agent_id = agent_id
        self.config = config
        self.logger = logger or logging.getLogger(self.__class__.__name__)
        self.metrics = AgentMetrics()
        
    @abstractmethod
    async def execute(self, input_data: Dict[str, Any]) -> Dict[str, Any]:
        """执行任务（子类必须实现）"""
        pass
    
    async def run(self, input_data: Dict[str, Any]) -> Dict[str, Any]:
        """运行 Agent（带错误处理和指标）"""
        start_time = datetime.now()
        
        try:
            self.logger.info(f"Agent {self.agent_id} 开始执行", extra={
                "agent_id": self.agent_id,
                "input": input_data
            })
            
            # 执行任务
            result = await self.execute(input_data)
            
            # 记录成功指标
            duration = (datetime.now() - start_time).total_seconds()
            self.metrics.record_success(duration)
            
            self.logger.info(f"Agent {self.agent_id} 执行成功", extra={
                "agent_id": self.agent_id,
                "duration": duration
            })
            
            return result
            
        except Exception as e:
            # 记录失败指标
            self.metrics.record_failure()
            
            self.logger.error(f"Agent {self.agent_id} 执行失败", extra={
                "agent_id": self.agent_id,
                "error": str(e),
                "error_type": type(e).__name__
            }, exc_info=True)
            
            raise
    
    def get_metrics(self) -> Dict[str, Any]:
        """获取性能指标"""
        return self.metrics.to_dict()

class AgentMetrics:
    """Agent 性能指标"""
    
    def __init__(self):
        self.total_executions = 0
        self.successful_executions = 0
        self.failed_executions = 0
        self.total_duration = 0.0
        self.avg_duration = 0.0
    
    def record_success(self, duration: float):
        self.total_executions += 1
        self.successful_executions += 1
        self.total_duration += duration
        self.avg_duration = self.total_duration / self.total_executions
    
    def record_failure(self):
        self.total_executions += 1
        self.failed_executions += 1
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            "total_executions": self.total_executions,
            "successful_executions": self.successful_executions,
            "failed_executions": self.failed_executions,
            "success_rate": self.successful_executions / max(self.total_executions, 1),
            "avg_duration": self.avg_duration
        }
```

---

## 三、配置管理

### 3.1 配置系统

```python
# src/utils/config.py
from typing import Any, Dict
import yaml
import os
from pathlib import Path

class Config:
    """配置管理器"""
    
    def __init__(self, env: str = None):
        self.env = env or os.getenv("ENV", "development")
        self.config_dir = Path("config")
        self._config: Dict[str, Any] = {}
        self._load_config()
    
    def _load_config(self):
        """加载配置文件"""
        # 1. 加载基础配置
        base_config_path = self.config_dir / "base.yaml"
        if base_config_path.exists():
            with open(base_config_path) as f:
                self._config = yaml.safe_load(f)
        
        # 2. 加载环境特定配置
        env_config_path = self.config_dir / f"{self.env}.yaml"
        if env_config_path.exists():
            with open(env_config_path) as f:
                env_config = yaml.safe_load(f)
                self._deep_update(self._config, env_config)
        
        # 3. 环境变量覆盖
        self._load_env_overrides()
    
    def _deep_update(self, base: dict, update: dict):
        """深度更新字典"""
        for key, value in update.items():
            if key in base and isinstance(base[key], dict) and isinstance(value, dict):
                self._deep_update(base[key], value)
            else:
                base[key] = value
    
    def _load_env_overrides(self):
        """从环境变量加载覆盖配置"""
        # 例如：APP_LLM__MODEL=gpt-4 -> config.llm.model = "gpt-4"
        prefix = "APP_"
        for key, value in os.environ.items():
            if key.startswith(prefix):
                config_path = key[len(prefix):].lower().split("__")
                self._set_nested(self._config, config_path, value)
    
    def _set_nested(self, d: dict, keys: list, value: Any):
        """设置嵌套字典值"""
        for key in keys[:-1]:
            d = d.setdefault(key, {})
        d[keys[-1]] = value
    
    def get(self, key: str, default: Any = None) -> Any:
        """获取配置值（支持点号路径）"""
        keys = key.split(".")
        value = self._config
        
        for k in keys:
            if isinstance(value, dict):
                value = value.get(k)
                if value is None:
                    return default
            else:
                return default
        
        return value
    
    def __getitem__(self, key: str) -> Any:
        return self.get(key)
    
    def __getattr__(self, name: str) -> Any:
        return self.get(name)

# 全局配置实例
config = Config()
```

### 3.2 配置文件示例

```yaml
# config/base.yaml
app:
  name: "AI Agent Service"
  version: "1.0.0"

llm:
  provider: "openai"
  model: "gpt-4"
  temperature: 0.7
  max_tokens: 2000
  timeout: 30

database:
  type: "postgresql"
  pool_size: 10

cache:
  type: "redis"
  ttl: 3600

logging:
  level: "INFO"
  format: "json"
```

```yaml
# config/production.yaml
llm:
  temperature: 0.3  # 生产环境使用更保守的温度

database:
  pool_size: 50

cache:
  ttl: 7200

logging:
  level: "WARNING"
```

---

## 四、日志系统

### 4.1 结构化日志

```python
# src/utils/logger.py
import logging
import json
from datetime import datetime
from typing import Any, Dict
import sys

class StructuredLogger:
    """结构化日志记录器"""
    
    def __init__(self, name: str, level: str = "INFO"):
        self.logger = logging.getLogger(name)
        self.logger.setLevel(getattr(logging, level.upper()))
        
        # 添加处理器
        handler = logging.StreamHandler(sys.stdout)
        handler.setFormatter(JSONFormatter())
        self.logger.addHandler(handler)
    
    def _log(self, level: str, message: str, **kwargs):
        """统一的日志记录方法"""
        extra = {
            "timestamp": datetime.utcnow().isoformat(),
            "level": level,
            **kwargs
        }
        
        getattr(self.logger, level.lower())(message, extra=extra)
    
    def info(self, message: str, **kwargs):
        self._log("INFO", message, **kwargs)
    
    def warning(self, message: str, **kwargs):
        self._log("WARNING", message, **kwargs)
    
    def error(self, message: str, **kwargs):
        self._log("ERROR", message, **kwargs)
    
    def debug(self, message: str, **kwargs):
        self._log("DEBUG", message, **kwargs)

class JSONFormatter(logging.Formatter):
    """JSON 格式化器"""
    
    def format(self, record: logging.LogRecord) -> str:
        log_data = {
            "timestamp": datetime.utcnow().isoformat(),
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
            "module": record.module,
            "function": record.funcName,
            "line": record.lineno
        }
        
        # 添加额外字段
        if hasattr(record, 'timestamp'):
            log_data.update({
                k: v for k, v in record.__dict__.items()
                if k not in ['name', 'msg', 'args', 'created', 'filename', 
                           'funcName', 'levelname', 'levelno', 'lineno',
                           'module', 'msecs', 'pathname', 'process',
                           'processName', 'relativeCreated', 'thread', 'threadName']
            })
        
        # 处理异常
        if record.exc_info:
            log_data['exception'] = self.formatException(record.exc_info)
        
        return json.dumps(log_data, ensure_ascii=False)

# 使用示例
logger = StructuredLogger("ai-agent")
logger.info("Agent 启动", agent_id="agent-001", model="gpt-4")
```

### 4.2 日志追踪

```python
import uuid
from contextvars import ContextVar

# 请求 ID 上下文
request_id_var: ContextVar[str] = ContextVar('request_id', default=None)

class RequestLogger:
    """带请求追踪的日志器"""
    
    def __init__(self, logger: StructuredLogger):
        self.logger = logger
    
    def log(self, level: str, message: str, **kwargs):
        """记录带请求 ID 的日志"""
        request_id = request_id_var.get()
        if request_id:
            kwargs['request_id'] = request_id
        
        getattr(self.logger, level)(message, **kwargs)

# 中间件：为每个请求生成 ID
async def request_id_middleware(request, call_next):
    request_id = str(uuid.uuid4())
    request_id_var.set(request_id)
    
    response = await call_next(request)
    response.headers['X-Request-ID'] = request_id
    
    return response
```

---

## 五、错误处理

### 5.1 自定义异常

```python
# src/utils/exceptions.py

class AgentException(Exception):
    """Agent 异常基类"""
    
    def __init__(self, message: str, code: str = None, details: Dict = None):
        self.message = message
        self.code = code
        self.details = details or {}
        super().__init__(self.message)

class LLMException(AgentException):
    """LLM 调用异常"""
    pass

class ToolException(AgentException):
    """工具执行异常"""
    pass

class ValidationException(AgentException):
    """数据验证异常"""
    pass

class RateLimitException(AgentException):
    """速率限制异常"""
    pass

class TimeoutException(AgentException):
    """超时异常"""
    pass
```

### 5.2 重试机制

```python
# src/utils/retry.py
import asyncio
from typing import Callable, Type, Tuple
from functools import wraps

def retry(
    max_attempts: int = 3,
    delay: float = 1.0,
    backoff: float = 2.0,
    exceptions: Tuple[Type[Exception], ...] = (Exception,)
):
    """重试装饰器"""
    
    def decorator(func: Callable):
        @wraps(func)
        async def wrapper(*args, **kwargs):
            attempt = 0
            current_delay = delay
            
            while attempt < max_attempts:
                try:
                    return await func(*args, **kwargs)
                except exceptions as e:
                    attempt += 1
                    
                    if attempt >= max_attempts:
                        raise
                    
                    logger.warning(
                        f"重试 {func.__name__}",
                        attempt=attempt,
                        max_attempts=max_attempts,
                        error=str(e)
                    )
                    
                    await asyncio.sleep(current_delay)
                    current_delay *= backoff
            
        return wrapper
    return decorator

# 使用示例
@retry(max_attempts=3, delay=1.0, exceptions=(LLMException, TimeoutException))
async def call_llm(prompt: str) -> str:
    """调用 LLM（带重试）"""
    response = await llm.agenerate(prompt)
    return response
```

### 5.3 熔断器

```python
# src/utils/circuit_breaker.py
from enum import Enum
from datetime import datetime, timedelta

class CircuitState(Enum):
    CLOSED = "closed"      # 正常
    OPEN = "open"          # 熔断
    HALF_OPEN = "half_open"  # 半开（测试恢复）

class CircuitBreaker:
    """熔断器"""
    
    def __init__(
        self,
        failure_threshold: int = 5,
        timeout: int = 60,
        recovery_timeout: int = 30
    ):
        self.failure_threshold = failure_threshold
        self.timeout = timeout
        self.recovery_timeout = recovery_timeout
        
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.last_failure_time = None
        self.last_attempt_time = None
    
    def call(self, func: Callable):
        """执行函数调用"""
        # 检查熔断器状态
        if self.state == CircuitState.OPEN:
            if self._should_attempt_reset():
                self.state = CircuitState.HALF_OPEN
            else:
                raise CircuitBreakerException("熔断器已打开")
        
        try:
            result = func()
            self._on_success()
            return result
        except Exception as e:
            self._on_failure()
            raise
    
    def _on_success(self):
        """成功回调"""
        self.failure_count = 0
        self.state = CircuitState.CLOSED
    
    def _on_failure(self):
        """失败回调"""
        self.failure_count += 1
        self.last_failure_time = datetime.now()
        
        if self.failure_count >= self.failure_threshold:
            self.state = CircuitState.OPEN
    
    def _should_attempt_reset(self) -> bool:
        """是否应该尝试恢复"""
        if self.last_failure_time is None:
            return True
        
        elapsed = (datetime.now() - self.last_failure_time).total_seconds()
        return elapsed >= self.recovery_timeout

# 使用示例
breaker = CircuitBreaker(failure_threshold=5, recovery_timeout=60)

def protected_call():
    return breaker.call(lambda: risky_operation())
```

---

## 六、测试

### 6.1 单元测试

```python
# tests/unit/test_agent.py
import pytest
from unittest.mock import Mock, AsyncMock
from src.agents.research_agent import ResearchAgent

@pytest.fixture
def mock_llm():
    """Mock LLM"""
    llm = AsyncMock()
    llm.agenerate.return_value = "这是测试响应"
    return llm

@pytest.fixture
def research_agent(mock_llm):
    """创建测试用的 Agent"""
    config = {"model": "gpt-4", "temperature": 0}
    return ResearchAgent(agent_id="test-agent", config=config, llm=mock_llm)

@pytest.mark.asyncio
async def test_research_agent_execute(research_agent, mock_llm):
    """测试 Agent 执行"""
    input_data = {"query": "什么是 AI Agent？"}
    
    result = await research_agent.execute(input_data)
    
    assert result is not None
    assert "response" in result
    mock_llm.agenerate.assert_called_once()

@pytest.mark.asyncio
async def test_research_agent_error_handling(research_agent, mock_llm):
    """测试错误处理"""
    mock_llm.agenerate.side_effect = Exception("API 错误")
    
    with pytest.raises(Exception):
        await research_agent.execute({"query": "test"})
```

### 6.2 集成测试

```python
# tests/integration/test_workflow.py
import pytest
from src.agents.research_agent import ResearchAgent
from src.agents.writer_agent import WriterAgent

@pytest.mark.integration
@pytest.mark.asyncio
async def test_research_to_writer_workflow():
    """测试完整工作流"""
    # 1. 研究阶段
    researcher = ResearchAgent(agent_id="researcher", config={})
    research_result = await researcher.execute({
        "query": "AI Agent 的应用"
    })
    
    # 2. 写作阶段
    writer = WriterAgent(agent_id="writer", config={})
    article = await writer.execute({
        "research_data": research_result["response"]
    })
    
    # 3. 验证结果
    assert article is not None
    assert len(article["content"]) > 100
```

### 6.3 性能测试

```python
# tests/performance/test_load.py
import asyncio
import time
from concurrent.futures import ThreadPoolExecutor

async def load_test(agent, num_requests: int = 100):
    """负载测试"""
    start_time = time.time()
    
    tasks = [
        agent.execute({"query": f"问题 {i}"})
        for i in range(num_requests)
    ]
    
    results = await asyncio.gather(*tasks, return_exceptions=True)
    
    duration = time.time() - start_time
    success_count = sum(1 for r in results if not isinstance(r, Exception))
    
    print(f"总请求数: {num_requests}")
    print(f"成功数: {success_count}")
    print(f"失败数: {num_requests - success_count}")
    print(f"总耗时: {duration:.2f}秒")
    print(f"QPS: {num_requests / duration:.2f}")
    print(f"平均延迟: {duration / num_requests * 1000:.2f}ms")
```

---

## 七、API 服务

### 7.1 FastAPI 实现

```python
# src/api/routes.py
from fastapi import FastAPI, HTTPException, Depends
from pydantic import BaseModel
from typing import Dict, Any
import uuid

app = FastAPI(title="AI Agent API", version="1.0.0")

class AgentRequest(BaseModel):
    agent_id: str
    input_data: Dict[str, Any]

class AgentResponse(BaseModel):
    request_id: str
    agent_id: str
    result: Dict[str, Any]
    execution_time: float

@app.post("/api/v1/agents/execute", response_model=AgentResponse)
async def execute_agent(request: AgentRequest):
    """执行 Agent"""
    request_id = str(uuid.uuid4())
    start_time = time.time()
    
    try:
        # 获取 Agent
        agent = get_agent(request.agent_id)
        
        # 执行
        result = await agent.run(request.input_data)
        
        return AgentResponse(
            request_id=request_id,
            agent_id=request.agent_id,
            result=result,
            execution_time=time.time() - start_time
        )
    
    except Exception as e:
        logger.error(f"Agent 执行失败", request_id=request_id, error=str(e))
        raise HTTPException(status_code=500, detail=str(e))

@app.get("/api/v1/health")
async def health_check():
    """健康检查"""
    return {"status": "healthy", "timestamp": datetime.utcnow().isoformat()}

@app.get("/api/v1/metrics")
async def get_metrics():
    """获取指标"""
    return {
        "agents": {
            agent_id: agent.get_metrics()
            for agent_id, agent in agent_registry.items()
        }
    }
```

---

## 八、监控和追踪

### 8.1 Prometheus 指标

```python
# src/utils/metrics.py
from prometheus_client import Counter, Histogram, Gauge

# 定义指标
agent_requests_total = Counter(
    'agent_requests_total',
    'Total number of agent requests',
    ['agent_id', 'status']
)

agent_request_duration = Histogram(
    'agent_request_duration_seconds',
    'Agent request duration',
    ['agent_id']
)

llm_tokens_used = Counter(
    'llm_tokens_used_total',
    'Total LLM tokens used',
    ['model', 'type']  # type: prompt or completion
)

# 使用
async def execute_with_metrics(agent_id: str, input_data: dict):
    with agent_request_duration.labels(agent_id=agent_id).time():
        try:
            result = await agent.execute(input_data)
            agent_requests_total.labels(agent_id=agent_id, status='success').inc()
            return result
        except Exception as e:
            agent_requests_total.labels(agent_id=agent_id, status='error').inc()
            raise
```

---

## 九、成本优化

### 9.1 Token 计数和限制

```python
import tiktoken

class TokenManager:
    """Token 管理器"""
    
    def __init__(self, model: str = "gpt-4"):
        self.encoding = tiktoken.encoding_for_model(model)
        self.max_tokens = 8000  # 设置限制
    
    def count_tokens(self, text: str) -> int:
        """计算 Token 数量"""
        return len(self.encoding.encode(text))
    
    def truncate_to_limit(self, text: str, max_tokens: int = None) -> str:
        """截断文本到指定 Token 数"""
        max_tokens = max_tokens or self.max_tokens
        tokens = self.encoding.encode(text)
        
        if len(tokens) <= max_tokens:
            return text
        
        truncated_tokens = tokens[:max_tokens]
        return self.encoding.decode(truncated_tokens)
    
    def estimate_cost(self, prompt_tokens: int, completion_tokens: int, model: str = "gpt-4") -> float:
        """估算成本（美元）"""
        pricing = {
            "gpt-4": {"prompt": 0.03 / 1000, "completion": 0.06 / 1000},
            "gpt-3.5-turbo": {"prompt": 0.0015 / 1000, "completion": 0.002 / 1000}
        }
        
        rates = pricing.get(model, pricing["gpt-4"])
        cost = (prompt_tokens * rates["prompt"] + 
                completion_tokens * rates["completion"])
        return cost
```

### 9.2 缓存策略

```python
from functools import lru_cache
import hashlib
import json

class ResponseCache:
    """响应缓存"""
    
    def __init__(self, redis_client):
        self.redis = redis_client
        self.ttl = 3600  # 1小时
    
    def _make_key(self, agent_id: str, input_data: dict) -> str:
        """生成缓存键"""
        data_str = json.dumps(input_data, sort_keys=True)
        hash_obj = hashlib.md5(data_str.encode())
        return f"agent:{agent_id}:{hash_obj.hexdigest()}"
    
    async def get(self, agent_id: str, input_data: dict):
        """获取缓存"""
        key = self._make_key(agent_id, input_data)
        cached = await self.redis.get(key)
        
        if cached:
            logger.info("缓存命中", agent_id=agent_id)
            return json.loads(cached)
        
        return None
    
    async def set(self, agent_id: str, input_data: dict, result: dict):
        """设置缓存"""
        key = self._make_key(agent_id, input_data)
        await self.redis.setex(
            key,
            self.ttl,
            json.dumps(result)
        )
```

---

## 十、运行步骤

```bash
# 1. 安装依赖
pip install -r requirements.txt
pip install -r requirements-dev.txt  # 开发依赖

# 2. 配置环境
export ENV=development
cp .env.example .env

# 3. 运行测试
pytest tests/ -v

# 4. 启动服务
uvicorn src.api.routes:app --reload --port 8000

# 5. 查看 API 文档
# 访问 http://localhost:8000/docs
```

---

## 十一、相关资源

- [[知识图谱]] - 完整学习路径导航
- [[11-部署教程]] - 部署方案
- [[12-运维监控教程]] - 运维监控
- [[04-生产环境检查清单]] - 生产环境最佳实践
- [[05-性能优化指南]] - 性能优化
- [[06-成本控制策略]] - 成本管理

---

## 十二、部署检查清单

- [ ] 配置管理：多环境配置完善
- [ ] 日志系统：结构化日志、日志级别正确
- [ ] 错误处理：异常捕获、降级策略
- [ ] 重试机制：LLM 调用、外部 API
- [ ] 熔断器：保护外部依赖
- [ ] 测试覆盖：单元测试、集成测试
- [ ] 性能优化：缓存、并发控制
- [ ] 监控指标：Prometheus 指标完善
- [ ] 成本控制：Token 限制、缓存策略
- [ ] 文档：API 文档、架构文档

---

**从原型到生产，构建可靠的 AI Agent 系统！**
