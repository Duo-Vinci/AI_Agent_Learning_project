# 部署教程

## 一、项目概述

本项目讲解如何将 AI Agent 应用部署到生产环境，涵盖容器化、CI/CD、云平台部署等主题。

**项目位置**：`projects/11-deployment/`

**学习目标**：
- 掌握 Docker 容器化
- 学习 CI/CD 流程设计
- 理解密钥和配置管理
- 掌握主流云平台部署
- 学习 Serverless 部署
- 理解负载均衡和高可用

---

## 二、Docker 容器化

### 2.1 Dockerfile

```dockerfile
# Dockerfile
FROM python:3.11-slim

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    build-essential \
    curl \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .

# 安装 Python 依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY src/ ./src/
COPY config/ ./config/

# 创建非 root 用户
RUN useradd -m -u 1000 appuser && \
    chown -R appuser:appuser /app
USER appuser

# 暴露端口
EXPOSE 8000

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8000/api/v1/health || exit 1

# 启动命令
CMD ["uvicorn", "src.api.routes:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 2.2 多阶段构建

```dockerfile
# Dockerfile.multistage
# 第一阶段：构建
FROM python:3.11-slim AS builder

WORKDIR /app

# 安装构建依赖
RUN apt-get update && apt-get install -y build-essential

COPY requirements.txt .
RUN pip install --user --no-cache-dir -r requirements.txt

# 第二阶段：运行时
FROM python:3.11-slim

WORKDIR /app

# 只复制必要的文件
COPY --from=builder /root/.local /root/.local
COPY src/ ./src/
COPY config/ ./config/

# 确保脚本在 PATH 中
ENV PATH=/root/.local/bin:$PATH

USER appuser
EXPOSE 8000

CMD ["uvicorn", "src.api.routes:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 2.3 Docker Compose

```yaml
# docker-compose.yml
version: '3.8'

services:
  # AI Agent 应用
  agent-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-api
    ports:
      - "8000:8000"
    environment:
      - ENV=production
      - REDIS_URL=redis://redis:6379
      - POSTGRES_URL=postgresql://postgres:password@postgres:5432/agentdb
    env_file:
      - .env
    depends_on:
      - redis
      - postgres
    restart: unless-stopped
    networks:
      - agent-network
    volumes:
      - ./logs:/app/logs

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: agent-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    networks:
      - agent-network

  # PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    container_name: agent-postgres
    environment:
      - POSTGRES_DB=agentdb
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - agent-network

  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    container_name: agent-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - agent-api
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge

volumes:
  redis-data:
  postgres-data:
```

### 2.4 构建和运行

```bash
# 构建镜像
docker build -t ai-agent:latest .

# 运行容器
docker run -d \
  --name agent-api \
  -p 8000:8000 \
  --env-file .env \
  ai-agent:latest

# 使用 Docker Compose
docker-compose up -d

# 查看日志
docker-compose logs -f agent-api

# 停止服务
docker-compose down

# 重启服务
docker-compose restart agent-api
```

---

## 三、CI/CD 流程

### 3.1 GitHub Actions

```yaml
# .github/workflows/ci-cd.yml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # 测试任务
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
    
    - name: Run linting
      run: |
        flake8 src/ --max-line-length=120
        black --check src/
    
    - name: Run tests
      run: |
        pytest tests/ -v --cov=src --cov-report=xml
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml

  # 构建镜像
  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Log in to Container Registry
      uses: docker/login-action@v2
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=sha,prefix={{branch}}-
          type=semver,pattern={{version}}
    
    - name: Build and push
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  # 部署到生产环境
  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deploy to production
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.PROD_HOST }}
        username: ${{ secrets.PROD_USER }}
        key: ${{ secrets.PROD_SSH_KEY }}
        script: |
          cd /opt/ai-agent
          docker-compose pull
          docker-compose up -d
          docker system prune -af
```

### 3.2 GitLab CI

```yaml
# .gitlab-ci.yml
stages:
  - test
  - build
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  IMAGE_TAG: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG

# 测试阶段
test:
  stage: test
  image: python:3.11
  before_script:
    - pip install -r requirements.txt -r requirements-dev.txt
  script:
    - pytest tests/ -v --cov=src
    - flake8 src/
  coverage: '/TOTAL.*\s+(\d+%)$/'

# 构建阶段
build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker build -t $IMAGE_TAG .
    - docker push $IMAGE_TAG
  only:
    - main
    - develop

# 部署阶段
deploy_production:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
  script:
    - ssh -o StrictHostKeyChecking=no $PROD_USER@$PROD_HOST "
        cd /opt/ai-agent &&
        docker-compose pull &&
        docker-compose up -d &&
        docker system prune -af"
  only:
    - main
  when: manual
```

---

## 四、密钥管理

### 4.1 环境变量

```bash
# .env.example
# API Keys
OPENAI_API_KEY=sk-xxx
DEEPSEEK_API_KEY=sk-xxx

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Redis
REDIS_URL=redis://localhost:6379

# Application
ENV=production
LOG_LEVEL=INFO
SECRET_KEY=your-secret-key

# Monitoring
SENTRY_DSN=https://xxx@sentry.io/xxx
```

### 4.2 AWS Secrets Manager

```python
# src/utils/secrets.py
import boto3
import json
from typing import Dict, Any

class SecretsManager:
    """AWS Secrets Manager 客户端"""
    
    def __init__(self, region_name: str = "us-east-1"):
        self.client = boto3.client(
            'secretsmanager',
            region_name=region_name
        )
    
    def get_secret(self, secret_name: str) -> Dict[str, Any]:
        """获取密钥"""
        try:
            response = self.client.get_secret_value(
                SecretId=secret_name
            )
            
            if 'SecretString' in response:
                return json.loads(response['SecretString'])
            else:
                return response['SecretBinary']
        
        except Exception as e:
            logger.error(f"获取密钥失败: {secret_name}", error=str(e))
            raise

# 使用
secrets = SecretsManager()
api_keys = secrets.get_secret("ai-agent/api-keys")
openai_key = api_keys['OPENAI_API_KEY']
```

### 4.3 Kubernetes Secrets

```yaml
# k8s/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: ai-agent-secrets
type: Opaque
stringData:
  OPENAI_API_KEY: "sk-xxx"
  DATABASE_URL: "postgresql://user:pass@host:5432/db"
  REDIS_URL: "redis://host:6379"
```

```yaml
# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-agent
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ai-agent
  template:
    metadata:
      labels:
        app: ai-agent
    spec:
      containers:
      - name: agent-api
        image: ghcr.io/username/ai-agent:latest
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: ai-agent-secrets
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
```

---

## 五、云平台部署

### 5.1 AWS (EC2 + ECS)

```bash
# 部署脚本
#!/bin/bash

# 1. 构建并推送镜像到 ECR
aws ecr get-login-password --region us-east-1 | \
  docker login --username AWS --password-stdin <account-id>.dkr.ecr.us-east-1.amazonaws.com

docker build -t ai-agent .
docker tag ai-agent:latest <account-id>.dkr.ecr.us-east-1.amazonaws.com/ai-agent:latest
docker push <account-id>.dkr.ecr.us-east-1.amazonaws.com/ai-agent:latest

# 2. 更新 ECS 服务
aws ecs update-service \
  --cluster ai-agent-cluster \
  --service ai-agent-service \
  --force-new-deployment
```

**ECS 任务定义**:

```json
{
  "family": "ai-agent-task",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "1024",
  "memory": "2048",
  "containerDefinitions": [
    {
      "name": "agent-api",
      "image": "<account-id>.dkr.ecr.us-east-1.amazonaws.com/ai-agent:latest",
      "portMappings": [
        {
          "containerPort": 8000,
          "protocol": "tcp"
        }
      ],
      "environment": [
        {
          "name": "ENV",
          "value": "production"
        }
      ],
      "secrets": [
        {
          "name": "OPENAI_API_KEY",
          "valueFrom": "arn:aws:secretsmanager:us-east-1:xxx:secret:api-keys"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/ai-agent",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
```

### 5.2 Google Cloud (Cloud Run)

```bash
# 部署到 Cloud Run
gcloud run deploy ai-agent \
  --image gcr.io/project-id/ai-agent:latest \
  --platform managed \
  --region us-central1 \
  --allow-unauthenticated \
  --set-env-vars ENV=production \
  --set-secrets OPENAI_API_KEY=openai-key:latest \
  --memory 2Gi \
  --cpu 2 \
  --max-instances 10 \
  --min-instances 1
```

### 5.3 Azure (Container Apps)

```bash
# 部署到 Azure Container Apps
az containerapp create \
  --name ai-agent \
  --resource-group ai-agent-rg \
  --environment ai-agent-env \
  --image youracr.azurecr.io/ai-agent:latest \
  --target-port 8000 \
  --ingress external \
  --cpu 1.0 \
  --memory 2.0Gi \
  --min-replicas 1 \
  --max-replicas 10 \
  --secrets openai-key=<value> \
  --env-vars ENV=production OPENAI_API_KEY=secretref:openai-key
```

### 5.4 阿里云 (ACK + ACR)

```bash
# 1. 推送镜像到 ACR
docker login --username=<username> registry.cn-hangzhou.aliyuncs.com
docker tag ai-agent:latest registry.cn-hangzhou.aliyuncs.com/namespace/ai-agent:latest
docker push registry.cn-hangzhou.aliyuncs.com/namespace/ai-agent:latest

# 2. 部署到 ACK
kubectl apply -f k8s/deployment.yaml
kubectl apply -f k8s/service.yaml
kubectl apply -f k8s/ingress.yaml
```

---

## 六、Serverless 部署

### 6.1 AWS Lambda

```python
# lambda_handler.py
import json
from mangum import Mangum
from src.api.routes import app

# 包装 FastAPI 应用
handler = Mangum(app)

def lambda_handler(event, context):
    """Lambda 入口函数"""
    return handler(event, context)
```

```yaml
# serverless.yml
service: ai-agent

provider:
  name: aws
  runtime: python3.11
  region: us-east-1
  memorySize: 2048
  timeout: 30
  environment:
    ENV: production
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - secretsmanager:GetSecretValue
          Resource: "arn:aws:secretsmanager:*:*:secret:*"

functions:
  api:
    handler: lambda_handler.handler
    events:
      - httpApi:
          path: /{proxy+}
          method: ANY

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    dockerizePip: true
    zip: true
```

### 6.2 Google Cloud Functions

```python
# main.py
from flask import Request
from src.api.routes import app

def agent_api(request: Request):
    """Cloud Function 入口"""
    with app.request_context(request.environ):
        return app.full_dispatch_request()
```

---

## 七、负载均衡和高可用

### 7.1 Nginx 配置

```nginx
# nginx.conf
upstream agent_backend {
    least_conn;  # 最少连接负载均衡
    server agent-api-1:8000 max_fails=3 fail_timeout=30s;
    server agent-api-2:8000 max_fails=3 fail_timeout=30s;
    server agent-api-3:8000 max_fails=3 fail_timeout=30s;
}

server {
    listen 80;
    server_name api.example.com;
    
    # 重定向到 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.example.com;
    
    # SSL 证书
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    
    # 日志
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    
    # 限流
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_req zone=api_limit burst=20 nodelay;
    
    location / {
        proxy_pass http://agent_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # 健康检查
        proxy_next_upstream error timeout http_500 http_502 http_503;
    }
    
    location /health {
        proxy_pass http://agent_backend/api/v1/health;
        access_log off;
    }
}
```

### 7.2 Kubernetes 水平扩展

```yaml
# k8s/hpa.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ai-agent-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai-agent
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
```

---

## 八、监控和日志

### 8.1 日志聚合 (ELK)

```yaml
# docker-compose.logging.yml
version: '3.8'

services:
  elasticsearch:
    image: elasticsearch:8.8.0
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - es-data:/usr/share/elasticsearch/data

  logstash:
    image: logstash:8.8.0
    volumes:
      - ./logstash.conf:/usr/share/logstash/pipeline/logstash.conf
    depends_on:
      - elasticsearch

  kibana:
    image: kibana:8.8.0
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch

volumes:
  es-data:
```

### 8.2 APM (Application Performance Monitoring)

```python
# 集成 Sentry
import sentry_sdk
from sentry_sdk.integrations.fastapi import FastApiIntegration

sentry_sdk.init(
    dsn=os.getenv("SENTRY_DSN"),
    integrations=[FastApiIntegration()],
    traces_sample_rate=0.1,  # 10% 追踪采样
    environment=os.getenv("ENV")
)
```

---

## 九、部署检查清单

- [ ] 代码审查和测试通过
- [ ] 环境变量和密钥配置正确
- [ ] Docker 镜像构建成功
- [ ] 数据库迁移完成
- [ ] 健康检查端点正常
- [ ] 日志系统配置完成
- [ ] 监控和告警设置
- [ ] SSL 证书配置
- [ ] 负载均衡配置
- [ ] 备份策略确认
- [ ] 回滚计划准备
- [ ] 文档更新

---

## 十、相关资源

- [[知识图谱]] - 完整学习路径导航
- [[10-生产级开发教程]] - 生产级实践
- [[12-运维监控教程]] - 运维监控
- [[04-生产环境检查清单]] - 生产环境最佳实践

---

## 十一、运行步骤

```bash
# 本地测试
docker-compose up -d
curl http://localhost:8000/api/v1/health

# 部署到生产
./scripts/deploy.sh production

# 查看部署状态
kubectl get pods
kubectl logs -f deployment/ai-agent

# 回滚
kubectl rollout undo deployment/ai-agent
```

---

**安全、可靠地将 AI Agent 部署到生产环境！**
